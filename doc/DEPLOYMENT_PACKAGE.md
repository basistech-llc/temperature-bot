# Deployment Package Documentation

The canonical deployment and package specification is
[`DEPLOYMENT.md`](DEPLOYMENT.md). It defines the endpoint inventory, immutable
application-release format, host-configuration boundary, activation and
rollback rules, and validation requirements.
