# Config for every non-production run: `make local-dev`, pytest, CI, and the
# build/install verification targets. Not just a test fixture: the app reads it
# at runtime in simulator mode, so keys here must match what app/ reads
# (e.g. location.latitude / location.longitude in app/weather.py).
#
location:
  latitude: 42.40183172533366
  longitude: -71.12495183944704
  zipcode: 02144
  city: somerville
hubitat:
  host: 10.2.3.51
ae200:
  host: 10.2.1.20
#
# Please put secrets in environment variables e.g. AQICN_TOKEN
# secrets:
#   airnow:
#     api_key: AT0
#   hubitat:
#     access_token: AT1
#   google:
#     air_quality_api_key: AT2
#   aqicn:
#     token: AT3
